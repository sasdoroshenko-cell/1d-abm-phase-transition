# Runtime dependencies for the 1D ABM phase-transition project.
# Versions are identical across all three hypothesis archives (no conflicts).
# Pinned for Python 3.10 (the environment the experiments were run on: 3.10.11).

tqdm~=4.64.0
matplotlib~=3.5.1
pandas~=1.4.2
numpy~=1.22.3
statsmodels~=0.13.2
scipy~=1.8.1
seaborn~=0.11.2
